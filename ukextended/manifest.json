{
  "name": "UK extended",
  "version": "1.1",
  "manifest_version": 2,
  "description": "UK extended keyboard (backquote acting as a dead key for grave accent)",
  "background": {
    "scripts": ["background.js"]
  },
  "permissions": [
    "input"
  ],
  "input_components": [
    {
      "name": "UK extended",
      "type": "ime",
      "id": "uk_extended_ime",
      "description": "UK extended keyboard (backquote acting as a dead key for grave accent",  // user visible description
      "language": "en-GB",  // The primary language this IME is used for
      "layouts": ["gb(extd)"]  // The supported keyboard layouts for this IME (gb(extd) is actually a plain UK keyboard)
    }
  ]
}
